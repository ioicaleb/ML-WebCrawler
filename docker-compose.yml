version: '3.8'

services:
  dashboard:
    build: .
    container_name: eric_dashboard_dev
    ports:
      - "8502:8502"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
